/rust
*.swp
*.exe
*.pdb
/.vscode/settings.json
/.vscode/launch.json
/.vscode/tasks.json

# -V adsmt smoke driver build artifact (.rlib emitted alongside
# `source/rust_verify_test/tests/*.rs` when running the smoke
# fixture by hand; the regular workspace build stages into
# `/rust`, but the manual smoke driver writes next to the test).
/source/libverus_smoke.rlib

# §3.5.H AOT prelude bank cache (baked artefacts, regenerable)
/source/target-verus/*/aot/
/target-verus/*/aot/
.claude-research-library/
