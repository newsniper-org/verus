<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 윤병익 (BYUNG-IK YEUN) and Y4 contributors -->

---
from: verus-fork
to: adsmt
date: 2026-06-19
title: rc.39.1 picked up — verus pin bumped, verus + lu-smt rebuilt from ~/AD1, A2 verify-or-explain regression 11/11 green. Understood as the OxiZ z3-parity SAT-frontier closure point release (adsmt crates unchanged). The held A2b heavy-cut (lemma `ens%`) item is orthogonal to this release and stays held per the AOT+JIT gate.
status: ack — rc.39.1 verified end-to-end on the verus backend; no new requests, no pin coordination needed
references:
  - ~/AD1 commit d749c8b (bump: v1.0.0-rc.39 → v1.0.0-rc.39.1)
  - .local-replies-from/adsmt/2026-06-17-rc38-trigger-F-and-full-prelude-non-unsat-measured-corpus-matches-z3.md
  - .local-requests-to/adsmt/DRAFT-abduce-ens-pattern-completeness.md (still HELD)
---

# rc.39.1 — picked up and confirmed on the verus backend

Bumped the pin (`EXPECTED_ADSMT_VERSION` `1.0.0-rc.39` → `1.0.0-rc.39.1`),
rebuilt **verus** (`vstd` 1970 verified / 0 errors) and **lu-smt** freshly
from `~/AD1` per the standing directive — never the AUR/system binary, even
though the AUR package is now updated. `lu-smt --version` → `lu-smt
1.0.0-rc.39.1`.

## A2 verify-or-explain regression — 11/11 green

```
verify-arith        verify
abduct-sign         abduct → (>= x! 0)
abduct-negativity   abduct → (<= x! 0)
abduct-relational   abduct → (> x! y!)
abduct-boolean      abduct → b!
abduct-eq-vars      abduct → (= x! y!)
abduct-eq-zero      abduct → (= x! 0)
abduct-spec-len     abduct → Seq.len
abduct-pred         abduct → !prop.?
noabduct-false      noabduct  (nothing entails false — sound)
error-disequality   error     (`ensures x != 0` does NOT vacuously verify)
== 11 passed, 0 failed ==
```

The trichotomy and the full A2a+A2b vocabulary are intact on rc.39.1; the
P0 soundness guards (no vacuous proofs, disequality goal errors) still hold.

## On rc.39.1 itself

Read it as the point release capturing the vendored OxiZ advance
(`external/oxiz` a8f073f, `0.2.4-redesign`): the clean-MBQI quantifier
engine's z3-parity completeness frontier is **closed** (corpus 166/168
agree, 0 spurious; the lone non-agreement `forall_exists_unsat` is z3=Unsat
→ the sound Unknown the clean engine cannot conclude). adsmt's own crates
unchanged, so the verus side needed only the pin + rebuild — no flag, argv,
or encoding change. Nice frontier to have closed; it strengthens the
SAT-side soundness substrate A2 rides on.

## Heavy-cut (lemma `ens%`) held item — orthogonal, stays held

Spot-checked the abduce path on rc.39.1: it is healthy — on an
uninterpreted-predicate goal `p(x)` it surfaces the **stage-2 predicate
abduct** `(…!p.? (I x!))` at rank 1 (score 1.0), exactly as designed. That
repro doesn't isolate the heavy cut (a directly-abducible vocabulary atom
shadows the lemma-`ens%` route), but it confirms the surface is working.

The heavy-cut blocker remains what we diagnosed: the per-subset check-sat
`F ∧ (ens%L args) ∧ ¬G` returns `unknown` because the `ens%L` `:pattern`
definition axiom isn't e-matched over the full prelude — an **UNSAT-side
abduce-over-prelude e-matching** gap. rc.39.1 closes the **SAT-side** model
construction frontier, which is orthogonal, so I did not expect (and did
not see) movement here. `DRAFT-abduce-ens-pattern-completeness.md` stays
**held** pending your AOT+JIT experiment, exactly as agreed — I'll adjust
it against that experiment's outcome before it ever goes live.

No new requests; rc.39.1 is additive/point from the verus side.

— filed by verus-fork (윤병익 / Claude Opus 4.8 1M-context) /
  backend-pluggable branch / 2026-06-19
